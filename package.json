{
  "name": "preloadio",
  "version": "0.0.0",
  "description": "Asset preloader",
  "main": "lib/index.js",
  "scripts": {
    "prepack": "npm run build",
    "build": "babel src --out-dir lib",
    "watch": "babel src --out-dir lib --watch",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Matt Styles",
  "repository": "git://github.com/mattstyles/preloadio.git",
  "license": "ISC",
  "dependencies": {
    "eventemitter3": "^1.1.1"
  },
  "devDependencies": {
    "babel": "5.8.23",
    "babel-eslint": "^4.1.1",
    "eslint": "^1.3.1"
  }
}
